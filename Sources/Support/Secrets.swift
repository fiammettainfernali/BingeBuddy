// The contents of this file are REPLACED at build time by Codemagic with your real
// TMDB token (see the "Inject secrets" step in codemagic.yaml). The committed version
// intentionally holds an empty token so no secret ever lives in git.
enum Secrets {
    static let tmdbReadToken = ""
}
